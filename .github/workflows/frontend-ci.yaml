name: Frontend CI & Docker Build

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: kinder-ap-fe

    env:
      IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/kinder-frontend
      IMAGE_TAG: ${{ github.run_number }}
      GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}

    steps:
      # ---------------- Checkout ----------------
      - name: Checkout code
        uses: actions/checkout@v4

      # ---------------- Node Setup ----------------
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      # ---------------- Install dependencies ----------------
      - name: Install dependencies
        run: npm ci

      # ---------------- Build React app ----------------
      - name: Build React app
        run: npm run build

      # ---------------- Docker Login ----------------
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # ---------------- Build & Push Docker Image ----------------
      - name: Build & Push Docker image
        run: |
          docker build -t $IMAGE_NAME:$IMAGE_TAG .
          docker push $IMAGE_NAME:$IMAGE_TAG

# # ---------------- Update Kubernetes Manifests ----------------
#       - name: Update K8s manifests in GitHub repo
#         env:
#           GIT_TOKEN: ${{ secrets.GIT_TOKEN }}
#         run: |
#           # Clone using GitHub token (username can be anything, token is the password)
#           git clone https://nasirnjs:${GIT_TOKEN}@github.com/nasirnjs/kinder-ci-k8s.git
          
#           cd kinder-ci-k8s
#           git config --global user.email "nasirnjs@gmail.com"
#           git config --global user.name "nasirnjs"
#           git checkout main

#           # Update deployment manifest image
#           sed -i "s|image:.*|image: $IMAGE_NAME:$IMAGE_TAG|g" kinder-app-fe.yaml

#           # Commit and push
#           git add kinder-app-fe.yaml
#           git commit -m "Update frontend image to build-$IMAGE_TAG" || echo "No changes to commit"
#           git push origin main
